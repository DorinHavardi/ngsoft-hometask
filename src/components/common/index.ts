import {ProgressBar as iProgressBar} from './../progress-bar/progressbar.cmp';
import {Button as iButton} from './button.cmp';
import {Input as iInput} from './input.cmp';
import {Dropdown as iDropdown} from './dropdown.cmp';
import {Header as iHeader} from './header.cmp';
import {TabBar as iTabBar} from './tab-bar.cmp';

export const Button = iButton;
export const Input = iInput;
export const Dropdown = iDropdown;
export const Header = iHeader;
export const TabBar = iTabBar;
export const ProgressBar = iProgressBar;
